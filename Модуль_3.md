# *demoexam 2026*
![image](https://github.com/Demomen-saveTF2/SA1-22demo2025/blob/main/topology.png)
## Задание 1

### Выполните импорт пользователей в домен au-team.irpo:

- В качестве файла источника выберите файл users.csv располагающийся в образе Additional.iso

- Пользователи должны быть импортированы со своими паролями и другими атрибутами

- Убедитесь, что импортированные пользователи могут войти на машину HQ-CLI

<br/>

<details>
<summary>Решение</summary>
<br/>

#### Импорт пользователей
Монтируем образ Additional.iso и 
```yml
mount -o loop /dev/sr0 /mnt/ -v
ls -l /mnt/
```
Посмотрим файл с пользователями
> Обратите внимание на пароль, чтобы его вписать в скрипт!! в данном решении пароль представлен - P@ssw0rd1
```yml
less /mnt/Users.csv
```
Импортируем пользователей в файл `users_fix.csv`, пропустив неисправные данные. 
```yml
iconv -f UTF-8 -t UTF-8//IGNORE /mnt/Users.csv > /root/users_fix.csv
```

Создаем баш-скрипт для импорта пользователей в самба.
```yml
cat << "EOF" > import.sh
#!/bin/bash

while IFS=';' read -r name fam role phone ou street zip city country pass; do
    username="${fam}_${name}"
    samba-tool user create "${username,,}" "P@ssw0rd1" --given-name="$name" --surname="$fam" --job-title="$role" --telephone-number="$phone"
    [[ -n "$ou" ]] && samba-tool ou create "OU=$ou" 2>/dev/null
    [[ -n "$ou" ]] && samba-tool user move "${username,,}" "OU=$ou" 2>/dev/null
done < users_fix.csv
EOF
```

Запускаем скрипт, он будет работать долго
```yml
bash import.sh
```

Для просмотра импортированных пользователей введем команду:
```yml
samba-tool user list
```

Для просмотра конкретного пользователя:
```yml
samba-tool user show <имя пользователя>
```

После импорта переходим на HQ-CLI и заходим под любым пользователем, вводим пароль **P@ssw0rd1**
</details>

<br/>

## Задание 3

### Перенастройте ip-туннель с базового до уровня туннеля, обеспечивающего шифрование трафика

- Настройте защищенный туннель между HQ-RTR и BR-RTR
  
- Внесите необходимые изменения в конфигурацию динамической маршрутизации, протокол динамической маршрутизации должен возобновить работу после перенастройки туннеля

- Выбранное программное обеспечение, обоснование его выбора и его основные параметры, изменения в конфигурации динамической маршрутизации отметьте в отчёте.

<br/>

<details>
<summary>Решение</summary>
<br/>

#### Настройка HQ-RTR

```yml
en
conf
crypto-ipsec ike enable
crypto-ipsec profile CIPROFILE ike-v2
mode tunnel
ike-phase1
proposal aes256-sha256-modp2048
auth pre-shared-key P@ssw0rd
exit

ike-phase2 
protocol esp 
proposal aes256-sha256 
local-ts 172.16.1.2
remote-ts 172.16.2.2
end

conf
crypto-map CMAP 10
match peer 172.16.2.2
set crypto-ipsec profile CIPROFILE 
end

conf
filter-map ipv4 FMAP 5
match gre host 172.16.1.2 host 172.16.2.2
set crypto-map CMAP peer 172.16.2.2
exit

filter-map ipv4 FMAP 10
match udp host 172.16.2.2 eq 4500 host 172.16.1.2 eq 4500
set crypto-map CMAP peer 172.16.2.2
exit


filter-map ipv4 FMAP 15
match any any any
set accept
exit

interface isp
set filter-map in FMAP 10
exit

interface tunnel.0
set filter-map in FMAP 10
end

wr mem
```

#### Настройка BR-RTR

```yml
en
conf
crypto-ipsec ike enable

crypto-ipsec profile CIPROFILE ike-v2
mode tunnel
ike-phase1
proposal aes256-sha256-modp2048
auth pre-shared-key P@ssw0rd
exit

ike-phase2 
protocol esp 
proposal aes256-sha256 
local-ts 172.16.2.2
remote-ts 172.16.1.2
exit
exit

crypto-map CMAP 10
match peer 172.16.1.2
set crypto-ipsec profile CIPROFILE 
exit

filter-map ipv4 FMAP 5
match gre host 172.16.2.2 host 172.16.1.2
set crypto-map CMAP peer 172.16.1.2
exit

filter-map ipv4 FMAP 10
match udp host 172.16.1.2 eq 4500 host 172.16.2.2 eq 4500
set crypto-map CMAP peer 172.16.1.2
exit

filter-map ipv4 FMAP 15
match any any any
set accept
exit

interface isp
set filter-map in FMAP 10
exit

interface tunnel.0
set filter-map in FMAP 10
end

wr mem
```

Для проверки:
```yml
show crypto-ipsec ike connections 
```

Можем проверить на ISP. Переходим к нему и скачиваем tcpdump:
```yml
apt-get update && apt-get install tcpdump -y
```

Проверяем:
```yml
tcpdump -i ens20
```
<img width="875" height="202" alt="Снимок экрана 2026-06-13 165950" src="https://github.com/user-attachments/assets/24e67859-429d-4f0f-b4d0-57e49d927d66" />


</details>
<br/>

## Задание 4

### Настройте межсетевой экран на маршрутизаторах HQ-RTR и BR-RTR на сеть в сторону ISP

- Обеспечьте работу протоколов http, https, dns, ntp, icmp или дополнительных нужных протоколов
  
- Запретите остальные подключения из сети Интернет во внутреннюю сеть.


<br/>

<details>
<summary>Решение</summary>
<br/>

#### Данное решение написано при помощи нейросети, я не гарантирую его правильность
На HQ-RTR и BR-RTR вводим следующее:
```yml
conf
filter-map ipv4 TO_ISP
match icmp any any
set accept
match udp any any eq 53
set accept
match tcp any any eq 53
set accept
match udp any any eq 123
set accept
match tcp any any eq 80
set accept
match tcp any any eq 443
set accept
exit
interface isp
set filter-map in TO_ISP
end
wr mem
```

Проверка фильтра:
```yml
show filter-map ipv4 TO_ISP
```

</details>

<br/>


## Задание 5

### Настройте принт-сервер cups на сервере HQ-SRV:

- Опубликуйте виртуальный pdf-принтер
  
- На клиенте HQ-CLI подключите виртуальный принтер как принтер по умолчанию.


<br/>

<details>
<summary>Решение</summary>
<br/>

#### На HQ-SRV скачиваем пакеты cups, добавляем в автозагрузку и делимся принтером:

```yml
apt-get update && apt-get install cups cups-pdf -y
systemctl enable --now cups
cupsctl --share-printers --remote-any

```

Проверим прослушку порта для cups:
```yml
ss -tulpn | grep "631"
```
<img width="1088" height="100" alt="Снимок экрана 2026-06-13 194010" src="https://github.com/user-attachments/assets/8bc7daf4-a40b-4c20-86e1-34e699552241" />


Работаем через графическую оболочку HQ-CLI. Проверьте, пингуется ли HQ-SRV.
при помощи samba-tool добавить необходимые записи на DNS-сервере BR-SRV, команда для этого представлена в [Модуль_2](https://github.com/demom99/demoexam2025/blob/main/%D0%9C%D0%BE%D0%B4%D1%83%D0%BB%D1%8C_2.md#%D0%B7%D0%B0%D0%B4%D0%B0%D0%BD%D0%B8%D0%B5-1). а так, можно попрбобовать подключиться по IP-адресу.

Заходим в браузер и переходим по адресу: (хз какой лучше, при тестировании у меня подключалось и по тому и по другому.)  
http://hq-srv:631  
https://hq-srv:631  

Переходим во вкладку `Administration`, далее нажимаем на `Add printer`
<img width="1360" height="716" alt="Снимок экрана 2026-06-13 204203" src="https://github.com/user-attachments/assets/32480698-87dc-4ac5-8710-bc1518bd2c04" />  


Выбираем из представленных принтер `CUPS-PDF (Virtual PDF printer)`
<img width="1360" height="700" alt="Снимок экрана 2026-06-13 204341" src="https://github.com/user-attachments/assets/f0865e7c-ad0d-4ef5-8bff-96d91b03a878" />  


Не знаю надо ли, но на всякий случай жмём галочку на `Share this printer`
<img width="1358" height="702" alt="Снимок экрана 2026-06-13 204523" src="https://github.com/user-attachments/assets/e78f99bb-a37d-42aa-8e1c-93a9cb22c91c" />  

И на последок жмём `Add printer`
<img width="1363" height="700" alt="Снимок экрана 2026-06-13 204630" src="https://github.com/user-attachments/assets/a4364972-042a-48d8-8137-054f17e6bd66" />  


Переходим во вкладку `Printers`, нажимаем на `CUPS-PDF` 
<img width="1363" height="705" alt="Снимок экрана 2026-06-13 204913" src="https://github.com/user-attachments/assets/b688ce98-1d9f-41e9-bd0d-c93b77e514c6" />  



Надимаем на `Maintenance`и в выпадающем списке выбираем `Print test page`
<img width="1361" height="706" alt="Снимок экрана 2026-06-13 204933" src="https://github.com/user-attachments/assets/4199652e-7dd4-4c10-8a6c-b726f5ed61e9" />  

Проверка печати пробных страниц на HQ-SRV:

```yml
ls -l /var/spool/cups/
```
<img width="566" height="157" alt="Снимок экрана 2026-06-13 205748" src="https://github.com/user-attachments/assets/f558bf33-2096-4cb0-9aa7-7a43db9bea7e" />  

</details>

<br/>
